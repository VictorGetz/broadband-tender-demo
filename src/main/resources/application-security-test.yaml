spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MSSQLServer
    username: devDBUser
    password:
server:
  port: 8487
  servlet:
    application-display-name: Broadband Tender Security Test
